version: 1
network: test
volumes:
  - name: workdir
    hostPath: .
pods:
  - name: pod1
    containers:
      - name: nginx
        image: nginx
        volumeMounts:
          - name: workdir
            mountPath: "/work-dir"